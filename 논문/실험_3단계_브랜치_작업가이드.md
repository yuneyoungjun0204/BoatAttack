# 3단계(어려움) 실험 — 브랜치 작업 가이드

> 대상: 리뷰 3단계(실험 필요) — ① Fig.11 보상 스케일 규명, ② MAPPO ablation, ③ 다중시드 통계, ④ LOS 게인 정당화.
> 실험 설계 자체는 [`실험_계획_ablation_다중시드.md`](실험_계획_ablation_다중시드.md) 참조. **본 문서는 "어느 브랜치에서, 어떻게 안전하게, 어떻게 구현하나"** 를 다룸.

---

## 1. 어느 브랜치로 가나
| | 브랜치 | 비고 |
|---|--------|------|
| **이동 대상** | **`two-USV-system`** | 논문 실험본. 결과(65%/15%)·보상·관측·차단망이 이 브랜치 기준 |
| 현재 작업 | `one-way_REFACTORING` | 논문 이후 진화(단동선 towing·클러스터 관측 등) → 논문과 다름 |

- **왜 two-USV-system?** ablation·다중시드는 논문의 방법/결과와 **동일 코드 기반**이어야 비교가 정합. `one-way_REFACTORING`에서 학습하면 관측·보상·기동이 달라 논문 수치와 직접 비교 불가.
- 확인: `git branch -a | grep two-USV-system`, 보상필드 정합은 [`논문_요약_정리.md`](논문_요약_정리.md) §대응 Git 브랜치 참조.

---

## 2. ★ 브랜치 이동 전 — 데이터 손실 방지 (필수)

> 배경 교훈: 과거 참사의 진짜 원인은 **브랜치 전환이 아니라 `git clean`** 이었음. 아래를 지키면 전환·복귀는 안전.

### 2-0. 사실관계 (왜 전환 자체는 안전한가)
- **아트 에셋(fbx/tga/psd/exr/png/obj/wav 등)은 ignore가 아니라 Git LFS로 추적됨** (`.gitattributes`, 385파일). → git이 버전 관리하므로 전환 시 올바른 버전으로 따라오고 복귀 시 원복. **손실 대상 아님.**
- **실제 ignore+디스크 상주 = `Library/`(Unity 캐시)** + NavMesh/Lightmap/Lighting/WarFX/스크린샷 등 **재생성 가능 산출물**. `git switch`는 untracked+ignored를 **건드리지 않음**. Library는 깨져도 재import로 복구되는 캐시(데이터 아님).
- `two-USV-system`은 현재 브랜치의 **조상**(현재가 24커밋 앞, 대상 0커밋 앞) → 복귀 시 머지·충돌 없이 깔끔.
- **진짜 위험 4가지만 피하면 됨**: ① `git clean -fdx`(절대 금지) ② 미커밋 변경 미처리 ③ LFS 객체 미수신(포인터화) ④ Unity 켠 채 전환.

### 2-1. 체크리스트 (이동 전)
1. **`git status`로 미커밋 확인.** 현재 untracked: `논문/` 폴더(.tex·MD·figures·hwp). 추적 파일 수정분이 있으면 함께 확인.
2. **`논문/` 폴더를 repo 밖에 백업.** 가장 확실한 안전장치:
   ```bash
   cp -r "c:/Users/ANSL/Desktop/BoatAttack/논문" "c:/Users/ANSL/Desktop/논문_backup_$(date +%Y%m%d)"
   ```
   - 이유: `논문/`은 untracked → 브랜치 전환 자체로는 안 지워지지만, 만약 `two-USV-system`에 동일 경로 추적파일이 있으면 **checkout이 거부/충돌**할 수 있음. 백업이 있으면 안심.
3. **미커밋 추적 변경은 커밋 또는 stash.**
   ```bash
   git stash push -u -m "one-way 작업 임시저장"   # -u: untracked 포함(논문/도 함께 보관됨)
   # 또는 커밋: git add -A && git commit -m "WIP: one-way 작업 저장"
   ```
   - ⚠ `stash -u`는 untracked(논문/)도 stash로 들어감 → 전환 후 `git stash pop`으로 복원. 헷갈리면 **2-2 방식(논문/만 별도 보관)** 권장.
4. **🚫 절대 금지: `git clean -fdx` / `-fd` / `-x`.** ignore된 `Library/`·생성 산출물과 미추적 파일이 통째로 삭제됨(과거 참사 원인). 어떤 경우에도 실행하지 말 것.
5. **전환 후 LFS 동기화 확인.** fbx 등이 텍스트 포인터로 보이면 객체 미수신 → `git lfs pull` 실행. (`git lfs ls-files`에 `*` 표시면 정상 보유.)
6. **Unity 에디터 종료 후 전환.** 실행 중 전환은 `.meta`/`Library`/`Temp` 충돌·잠금 유발. 전환 후 Unity가 자동 재import(수 분 소요). 깨지면 `Library/` 삭제 후 재오픈(캐시 재생성).
6. `One-Way_Towing/boatattack_sim`(별도 폴더/Python)은 **다른 작업트리** → BoatAttack 브랜치 전환과 무관. 영향 없음.

### 2-2. 가장 안전한 권장 절차 (논문/ 분리 보관)
```bash
cd "c:/Users/ANSL/Desktop/BoatAttack"
git status                                  # 1) 상태 확인
cp -r 논문 ../논문_backup_yyyymmdd           # 2) 논문 백업(repo 밖)
git add -A && git commit -m "WIP 저장"        # 3) 추적 변경 커밋 (논문/도 커밋해두면 전환 후에도 유지)
git switch two-USV-system                    # 4) 전환 (switch는 checkout보다 안전)
# 5) Unity로 프로젝트 열어 재import 완료 대기
```
- `git switch`는 브랜치 전용 명령이라 실수로 파일을 덮어쓸 위험이 `checkout`보다 적음.
- 전환 후 충돌 메시지가 뜨면 **강제(-f) 쓰지 말고** 어떤 파일이 막는지 확인 → 백업본과 대조.

### 2-2b. ★ 권장: 작업 폴더를 안 건드리는 worktree (무위험)
> `one-way_REFACTORING`(현재 주력 브랜치)는 **절대 망가지면 안 됨** → 이 폴더에서 `git switch`를 하지 말고, 별도 폴더에 실험 브랜치를 펼친다.
```bash
cd "c:/Users/ANSL/Desktop/BoatAttack"
git worktree add ../BoatAttack_twoUSV two-USV-system    # 별도 폴더에 체크아웃
# → 현재 폴더는 one-way_REFACTORING 그대로 유지(파일·Library·LFS 손 안 댐)
# 새 폴더에서 Unity 열기:
#   - 첫 import 오래 걸림(Library 없음)
#   - fbx 포인터면: cd ../BoatAttack_twoUSV && git lfs pull
# 실험 끝나면:
git worktree remove ../BoatAttack_twoUSV                 # 정리(변경 없을 때)
```
- 장점: 현재 작업 브랜치 폴더를 **물리적으로 분리** → 전환·복귀로 인한 손상 가능성 0.
- 주의: worktree는 같은 repo의 LFS/objects를 공유하나 작업 디렉터리는 독립. `git clean`은 여기서도 금지.

### 2-3. 전환이 막힐 때
- "Your local changes would be overwritten" → 해당 파일 커밋/stash 후 재시도.
- "untracked working tree files would be overwritten" → 그 파일을 다른 이름으로 옮기고 전환(삭제 X).

---

## 2-9. 실측 환경 정보 (worktree 점검 결과)
> `git worktree add ../BoatAttack_twoUSV two-USV-system` 완료. 아래는 그 폴더 실측값.

| 항목 | 값 |
|------|-----|
| worktree 폴더 | `C:/Users/ANSL/Desktop/BoatAttack_twoUSV` (HEAD 80d7d9d1) |
| 학습 스크립트 | `Assets/Scripts/Defense/DefenseEnvController.cs`, `DefenseRewardCalculator.cs` |
| 학습 씬 | `Assets/scenes/ROONSHOOT.unity`, `Assets/scenes/ROONSHOOT_MultiAgent.unity` |
| ML-Agents(Unity) | release_20 — `C:/Users/ANSL/Desktop/CamearTest/ml-agents-release_20/com.unity.ml-agents` (manifest.json이 이 경로 참조) |
| 학습 config | `config/defense_boat_trainer.yaml`(메인) + selfplay/stage6/stageA/stageB |
| **behavior 이름** | **`Defence`**(poca), `Commander`(ppo) — ablation은 `Defence`의 trainer_type 변경 |
| 기존 결과(TB로그) | `C:/Users/ANSL/Desktop/CamearTest/ml-agents-release_20/results/` (0219~0304_full) → Fig.11 점검에 활용 |
| ⚠ Python | 기본 쉘 python에 `mlagents` 없음 → **학습용 venv/conda 활성화 필요**(release_20 호환: mlagents 0.30 계열) |

### ✅ Python 학습 환경 (구축 완료)
- **venv**: `C:/Users/ANSL/Desktop/CamearTest/ml-agents-release_20/venv` (Python 3.10.0)
- 활성화: git-bash `source .../venv/Scripts/activate` / PowerShell `& .../venv/Scripts/Activate.ps1`
- 검증됨: `mlagents-learn --help` 동작, **torch 2.0.1+cu118, CUDA True (RTX 4070)**
- 구성(재현용): torch 2.0.1+cu118 / numpy 1.23.5 / protobuf 3.19.6 / **setuptools 65.5.0**(82는 pkg_resources 제거되어 mlagents 실패→다운그레이드) / cattrs 1.5.0 / gym 0.26.2 / pettingzoo 1.15.0(--no-deps) / tensorboard 2.10.1 / pypiwin32 223 / mlagents·mlagents-envs는 release_20 소스 `-e --no-deps`
- 핵심 교훈: ① release_20 권장 torch≤1.11은 RTX 4070(sm_89) 미지원 → torch 2.0.1로 대체 ② setuptools≥81은 `pkg_resources` 삭제 → mlagents 0.30 import 실패 → setuptools<81 필수 ③ 의존성 핀 충돌은 `-e --no-deps` + 수동설치로 회피

### 선행 준비 (테스트 전)
1. **Unity로 worktree 열기**: Unity Hub → Add → `BoatAttack_twoUSV` 폴더. 첫 실행 시 Library 재생성·LFS smudge로 수~수십 분.
2. **씬 열기**: `ROONSHOOT.unity`(또는 `_MultiAgent`) → `DefenseEnvController`가 붙은 환경 확인. Inspector에서 `currentStage`·`activePairCount` 등 논문 시점 설정 확인.
3. **Python 환경**: 이전 학습에 쓴 venv 활성화(없으면 release_20에서 `pip install -e ./ml-agents-envs && pip install -e ./ml-agents`). `mlagents-learn --help`로 확인.
4. **스모크 테스트**: `mlagents-learn config/defense_boat_trainer.yaml --run-id=smoke_test` → Unity ▶ → 몇 스텝 도는지만 확인 후 중단(파이프라인 검증).

## 3. two-USV-system에서 할 작업 (우선순위 순)

### 3-1. Fig.11 보상 스케일 규명 (가벼움 — 가장 먼저)
- 목적: 누적보상 y축 ≈2.4×10⁻⁵ 원인 규명·정정(리뷰 K).
- 작업:
  1. 기존 학습 로그 위치 확인: `results/<run-id>/` (TensorBoard event 파일).
  2. 스칼라 덤프 → `Environment/Cumulative Reward` 실제 범위 확인.
     ```python
     from tensorboard.backend.event_processing.event_accumulator import EventAccumulator
     ea = EventAccumulator("results/<run-id>"); ea.Reload()
     print(ea.Tags()["scalars"])
     vals=[s.value for s in ea.Scalars("Environment/Cumulative Reward")]
     print(min(vals), max(vals))
     ```
  3. 이벤트(±1) 포함 시 0.1~수 범위가 정상 → 현재 그래프가 비정상이면 **축 라벨/지표 오류**로 판단, 재플롯.
- 산출: 정정된 Fig.11 또는 "지표 정의 명확화" 문구.

### 3-2. MAPPO Ablation (중간)
- config 2벌 준비: `config/defense_boat_trainer.yaml` 복제.
  - `poca_defense.yaml`: `trainer_type: poca` (현행)
  - `ppo_defense.yaml`: `trainer_type: ppo` (나머지 하이퍼 동일)
- 동일 `max_steps`로 각각 학습 → 평가(3-4 프로토콜) → 포획률·붕괴율·학습안정성 비교.
- 핵심 메시지: 에이전트 **중도 이탈(포획완료/충돌/대형붕괴)** 빈발 환경에서 MA-POCA가 PPO 대비 우위인지 정량 입증.

### 3-3. 다중시드 통계 (무거움)
- 각 조건 5시드: `--seed 1..5`, `--run-id poca_s1..s5` / `ppo_s1..s5`.
- 시드별 지표 → 평균±표준편차, MA-POCA vs (PPO/LOS) t-test/Mann–Whitney(n=5).

### 3-4. 공통 평가 프로토콜
- 각 학습 모델(.onnx, 추론 모드) → **고정 40 에피소드 × 동일 적 포메이션 분포**.
- 지표: Capture / Penetration / Formation collapse rate (논문 정의 동일).
- LOS 게인(Kp,Kd) 스윕 1~2종 추가 평가 → "게인 민감도 낮음/baseline 합리적" 근거(리뷰 J).

---

## 4. 구현 디테일
- **config 위치**: `config/defense_boat_trainer.yaml` (트레이너·하이퍼). Stage·보상은 Inspector(`DefenseEnvController`/`DefenseRewardCalculator`).
- **학습**:
  ```bash
  mlagents-learn config/poca_defense.yaml --run-id=poca_s1 --seed 1
  mlagents-learn config/ppo_defense.yaml  --run-id=ppo_s1  --seed 1
  tensorboard --logdir=results
  ```
- **가속**: `time_scale 10`(20+는 Gerstner 파도 불안정), 씬 내 환경 인스턴스 복제로 병렬.
- **평가 스크립트**(신규 작성): 학습된 `.onnx`를 Inference로 로드 → 40에피 루프 → 종료사유 카운트(capture/breach/collapse) → CSV. 시드별 집계 → mean±std → 막대그래프(에러바).
- **재현성**: 보상·k·환경 값은 논문 표(`ICROS_2026.tex` Table 3) 기준 — formation α_f 0.002, approach 0.001, heading 0.0003, k_e 77/k_p 100, 활성간격 [20,120]m, 적스폰 1000m.

---

## 5. 결과 → 논문 반영
- `ICROS_2026.tex` §학습 결과:
  - "향후 과제(단일시드·고정게인)" 문구 → **ablation 표 + 다중시드 mean±std 표/그림**으로 대체.
  - Fig.11 정정본 반영.
- 정합성: 방법은 이미 개선본((sin,cos)) 서술 → 재학습 결과로 65%/15% 수치 갱신 시, 표·본문 동시 수정.

## 6. 실험 후 복귀
1. 결과/모델/로그 커밋: `git add -A && git commit -m "3단계 실험 결과"` (results/는 용량 크면 별도 보관).
2. `git switch one-way_REFACTORING` (동일하게 git clean 금지·Unity 종료 후).
3. 백업해둔 `논문/`이 최신인지 확인 후 병합.

## 7. 빠른 명령 모음
```bash
# 이동 전
git status && cp -r 논문 ../논문_backup_yyyymmdd
git add -A && git commit -m "WIP"
git switch two-USV-system
# 실험
mlagents-learn config/poca_defense.yaml --run-id=poca_s1 --seed 1
# 복귀
git add -A && git commit -m "실험 결과"; git switch one-way_REFACTORING
#  절대 금지: git clean -fdx
```
